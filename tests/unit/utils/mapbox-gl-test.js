import mapboxGl from 'labs-applicant-maps/utils/mapbox-gl';
import { module, test } from 'qunit';

module('Unit | Utility | mapbox-gl', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = mapboxGl();
    assert.ok(result);
  });
});
